# Experiment1

Aim
To create a simple Android application with the help of Android Studio.

To know the basic structure of an Android application.

Say the phrase "Hello World" first thing when testing your device's voice display feature. A message will then appear right there on the mobile screen after those spoken words.

Finding out how layouts in XML connect with code in Java. One part shapes the design, the other handles actions. When they work together, screens come alive. Building apps starts here, piece by piece.

To know the basics of working with TextView in Android application UI design.

To know the basics of running an Android application.

To know the basics of working with MainActivity.java in an Android application.

To know the basics of working with activity_main.xml in an Android application.

To know the basics of working with Constraint Layout in Android application UI design.

A new beginning takes place at launch, driven by onCreate(). From there, movement starts without delay. Next, steps roll forward one after another. As the system signals, progress shifts naturally. Here is where it all settles into place. Setup becomes the base, quietly shaping what follows. Once it starts up, what happens next adjusts to each move you make. Depending on your choices, different outcomes appear. Each step forward shows a new condition. Underneath, systems track what's active and seen. How things behave grows from knowing where they stand.




Algorithm

Start things off with turning on the machine, after which you will launch Android Studio. Only once everything shows up on screen should you proceed further. The next step waits until the full layout settles into place.

A new task kicks off the moment you pick Create New Project. This choice shows up fast - just one tap brings it into view. Go ahead, make that selection to proceed. From there, everything shifts into the next stage. Right then, that’s all it takes

This round pick the choice labeled "Empty Activity" once that box appears on screen.

A fresh start begins with what to name the app - go ahead, pick Hello World. That exact phrase shows up on screen. Write each character as written. There is no extra step waiting behind. Just let the heading sit by itself right here

Start typing when the screen lights up. Select Java once it appears below.

Picking the right "Minimum SDK" matters more now than before. When the option shows up, think through what works best. Your choice here shapes how things run later.

Finish button clicked

A fresh project begins here. Right from the first move, its structure takes shape. Out of silence, pieces fall into place. When this phase ends, motion spreads through every part.

Open the res/layout folder first. Find activity_main.xml inside it next. Bring up the file at once. Editing can start because the layout waits there. Go to your project panel to get in. All interface shapes hide right here in this XML.

Here things shift under one rule: placement matters. The frame called ConstraintLayout steps in, guiding where each piece lands. Because it defines edges, nothing drifts past limits. Where a widget sits ties directly to connections drawn within. Each element listens - bound by what the layout decides. From here on, it takes complete charge of layout. The moment you add something, positioning stops changing.

Step 11: Now add "TextView" inside the layout.

Hello World

Number thirteen follows. To center the TextView, define boundaries on all sides. Right in the middle - that is where it stays. Held firm by constraints, never drifting toward an edge. Once both sides match up, the layout locks into place. Edges line up just right, then it stops moving. That stillness comes from balance. Each rule met means no more adjustments.

Inside the java directory, look for a folder named com.example.helloworld. There you will find MainActivity.java sitting quietly. It appears only once earlier steps finish completely. Waiting means it does nothing until someone opens it. Editing becomes possible the moment it loads.

Step 15: Now add "setContentView(R.layout.activity_main)" inside the "onCreate()" method.

Once inside the onCreate() method, slip in EdgeToEdge.enable(this). The moment it's there, things shift slightly.

A spot within the onCreate method is where you put ViewCompat.setOnApplyWindowInsetsListener. Right now marks step seventeen, so that task comes up here.

One by one, tuck each file into the project folder. Begin at the start, slotting things where they fit. Don’t stop until every piece rests inside. Only then call it done.

Once the device powers on, connect your Android phone or open the testing display. Proceed when ready. Finish up after the system responds.

Press the Run button inside Android Studio now. Then move forward right away.

Right where it takes shape. Straight into the connected Android device the app flows. Once compiling ends, deployment kicks in without delay. Fresh build lands on the device, running by itself.